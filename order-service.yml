spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5432/order_master
    driver: org.postgresql.Driver
    username: postgres
    password: holy@1234
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
